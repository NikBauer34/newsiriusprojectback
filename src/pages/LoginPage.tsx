import { Box } from "@mantine/core";
import React, { FC } from "react";

const Login: FC = () => {
    return(
        <Box maw={340} mx= 'auto'>
            <form>

            </form>
        </Box>
    );
};

export default Login;